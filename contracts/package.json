{
  "name": "@land-registry/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "forge test",
    "coverage": "forge coverage",
    "gas": "forge test --gas-report",
    "build": "forge build",
    "clean": "forge clean",
    "deploy:amoy": "forge script script/Deploy.s.sol --rpc-url amoy --broadcast",
    "seed:amoy": "forge script script/SeedParcels.s.sol --rpc-url amoy --broadcast"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "workspace:*",
    "forge-std": "workspace:*"
  }
}
